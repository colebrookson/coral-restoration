## notes about how to set up compute canada runs
### files
- bsah script to run a loop that uses 'do' to call a job with i, and denotes where the job is
- ariel had a bunch of csv files, it pulls the file, and it emails you when it begins a file, etc etc 
- look up compute canada to see what module to load in the bash
- then there's a parallelization part - it takes fr







